# Runtime dependencies.
#
# passlib[bcrypt]==1.7.4 and bcrypt==4.0.1 MUST stay on these exact versions:
# passlib 1.7.4 is incompatible with bcrypt>=4.1 (bcrypt.__about__ was
# removed, so verify_password raises AttributeError). Do not bump either one
# without replacing/upgrading passlib first.
#
# elasticsearch stays on a range: 8.x client/server compatibility is
# deliberately loose, and 9.x is a breaking major.
#
# aiomysql is required (Settings.async_database_url builds mysql+aiomysql://).

# Core web / API
fastapi==0.141.1
uvicorn[standard]==0.53.0
python-multipart==0.0.32
orjson==3.12.0

# Database
sqlalchemy==2.0.54
pymysql==1.2.3
aiomysql==0.3.2

# Search
elasticsearch>=8.11.0,<9.0.0

# Auth / crypto
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Config / validation
python-dotenv==1.2.3
pydantic==2.13.5
pydantic-settings==2.15.0

# Scheduling / HTTP client
apscheduler==3.11.3
httpx==0.28.1
