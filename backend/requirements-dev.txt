# Development / test dependencies — not installed in the runtime image.
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0,<9.0
pytest-cov>=5.0,<8.0
httpx>=0.27.0,<1.0.0
ruff>=0.6.0,<1.0.0
